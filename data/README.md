# Data Directory

No image data are included in this repository.

Use a separate, non-version-controlled working directory for FLIR JPEGs, extracted thermal PNGs, masks, and analysis outputs. Thermal images may contain identifying or sensitive information; publish them only when you have appropriate rights, consent, and de-identification.

The repository `.gitignore` excludes common image and generated-data formats under this directory by default.
